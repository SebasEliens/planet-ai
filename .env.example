# Copy to .env for local runs. In CI these come from repository secrets.

# LLM + web search, via OpenRouter (https://openrouter.ai/keys)
OPENROUTER_API_KEY=

# Optional: dedicated search provider instead of OpenRouter's web plugin
TAVILY_API_KEY=
BRAVE_SEARCH_API_KEY=
